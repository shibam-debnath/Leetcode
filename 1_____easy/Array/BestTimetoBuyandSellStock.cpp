// Shibam Debnath
// May 16,2022
#include <bits/stdc++.h>
using namespace std;
#define ll long long
int maxProfit(vector<int> &p)
{
    int n = p.size();
    int dp[n + 1][n + 1];
    int min = ;
    int sum = 0;
    for (int j = 2; j <= n; j++)
    {
    }
    for (int i = 1; i <= n; i++)
    {
        for (int j = 2; j <= n; j++)
        {
            if ()
        }
    }

    // int profit = 0;
    // int l = 0;
    // int r = p.size();
    // int val = 0;
    // while (l<r)
    // {
    //     val = p[l] - p[r];
    //     if(val > profit){
    //         profit = val;
    //     }
    //     if(p[l+1] < p[l]){
    //         l++;
    //         continue;
    //     }
    //     if(p[r-1] > p[r]){
    //         r--;
    //         continue;
    //     }

    //     l++;
    //     r--;

    // }

    // return profit;
}

int main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    return 0;
}
